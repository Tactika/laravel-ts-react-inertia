import React from "react";

interface HeroProps {}

const Hero: React.FC<HeroProps> = (props) => {
    return (
        <div>
            <h2>Hero Component</h2>
        </div>
    )
    };

export default Hero;
